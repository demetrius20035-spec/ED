# Git не трогает содержимое файлов ВООБЩЕ: никаких преобразований концов строк
# при коммите и выгрузке. Байты в рабочей папке = байты в репозитории = байты
# в ZIP-архиве с GitHub. Контрольные суммы совпадают на любой ОС.
* -text
