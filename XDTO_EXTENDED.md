# Расширенный пакет XDTO для EnterpriseData 1.6

Как дорабатывать формат обмена в расширении конфигурации, не трогая типовой пакет
`EnterpriseData_1_6_23` (`http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6`).

## Суть проблемы

Тип XDTO идентифицируется парой **«URI пространства имён + имя типа»**. Два типа с
одинаковым именем и одинаковой структурой, но в разных пакетах — это два разных,
несовместимых типа. Поэтому объект, созданный по типу из одного пакета, нельзя
присвоить свойству, типизированному одноимённым типом из другого пакета:
платформа выдаёт ошибку несоответствия типов.

Штатное решение — **директива импорта + наследование типов**.

## Памятка: как добавить объект в выгрузку (три сценария)

Порядок всегда один: **схема (XSD) → пакет в конфигураторе → код → проверка**.
Код всегда создаёт значения через `СоздатьЗначениеXDTO()` и пишет объекты через
`ЗаписатьОбъектXDTO()` — эти две функции сами разбираются с пространствами имён.

### Сценарий 1. Объект целиком описан типовым пакетом

Пакет расширения и XSD не трогаем вообще.

1. Найти в `EnterpriseData_1_6_23.xsd` тип объекта (`Документ.X` / `Справочник.X`)
   и его `КлючевыеСвойства…`. Выписать обязательные элементы (без `minOccurs="0"`) —
   их нужно заполнять всегда, иначе запись упадёт.
2. Написать функцию выгрузки по образцу существующих:

```bsl
Функция ПолучитьДанные_МойДокумент(ДокументСсылка, ФайлОбмена, КЭШ)

	ДанныеXDTO = СоздатьЗначениеXDTO("Документ.МойДокумент");

	КлючевоеСвойство = СоздатьЗначениеXDTO("КлючевыеСвойстваМойДокумент");
	КлючевоеСвойство.Ссылка = "" + ДокументСсылка.УникальныйИдентификатор();
	КлючевоеСвойство.Дата   = ДокументСсылка.Дата;
	КлючевоеСвойство.Номер  = ДокументСсылка.Номер;
	КлючевоеСвойство.Организация = ПолучитьКлючевоеСвойство(ДокументСсылка.Организация, ФайлОбмена, КЭШ, ДокументСсылка);
	ДанныеXDTO.КлючевыеСвойства = КлючевоеСвойство;

	// обязательные свойства присваиваем напрямую,
	// необязательные — через УстановитьДанные (пропускает пустые значения)
	УстановитьДанные(ДанныеXDTO, "Комментарий", ДокументСсылка.Комментарий);

	// табличная часть: контейнер -> строки -> присвоить свойству
	СтрокиXDTO = СоздатьЗначениеXDTO("Документ.МойДокумент.Товары");
	Для Каждого Строка Из ДокументСсылка.Товары Цикл
		СтрокаXDTO = СоздатьЗначениеXDTO("Документ.МойДокумент.Товары.Строка");
		// ... заполнение полей строки ...
		СтрокиXDTO.Строка.Добавить(СтрокаXDTO);
	КонецЦикла;
	УстановитьДанные(ДанныеXDTO, "Товары", СтрокиXDTO);

	Возврат ДанныеXDTO;

КонецФункции
```

3. Добавить ветку в `ВыгрузитьДанные`:

```bsl
ИначеЕсли ТипЗнч(Документ) = Тип("ДокументСсылка.МойДокумент") Тогда
	ДанныеXDTO = ПолучитьДанные_МойДокумент(Документ, ФайлОбмена, КЭШ);
```

4. Если на объект ссылаются другие выгружаемые объекты — добавить функцию
   `СоздатьКлючевоеСвойство_МойОбъект` (по образцу существующих: создать ключевое
   свойство, попутно выгрузить сам объект в файл, вернуть ключевое свойство)
   и ветку в `СоздатьКлючевоеСвойство` — кэш подхватится автоматически.

### Сценарий 2. Объект целиком описан расширенным пакетом (пример — Проекты)

1. В `EnterpriseData_1_6_23_Extended.xsd` добавить:
   - `КлючевыеСвойства<Объект>` (минимум `Ссылка` + `Наименование`);
   - тип объекта `Справочник.<Объект>` / `Документ.<Объект>` — наследник `ns2:Object`
     из схемы Message (необязательно для работы выгрузки, но так устроен типовой формат),
     свойство `КлючевыеСвойства` + реквизиты;
   - поля, ссылающиеся на типовые структуры, типизировать импортированными
     `ns1:`-типами (например `ns1:КлючевыеСвойстваОрганизация`) — не копиями;
   - перечисления — `xs:simpleType` + `xs:enumeration`;
   - всем условно-заполняемым полям — `minOccurs="0"`.
2. Переимпортировать пакет в расширение (оба XSD в одном каталоге).
3. Код — в точности как в сценарии 1: `СоздатьЗначениеXDTO` сам возьмёт тип из
   расширенного пакета. Ветка в `ВыгрузитьДанные`, при необходимости — своё
   ключевое свойство в `СоздатьКлючевоеСвойство`.

### Сценарий 3. Смешанный: типовой объект + реквизиты из расширения

1. В `EnterpriseData_1_6_23_Extended.xsd` создать **наследника** типового типа:
   то же локальное имя, `xs:extension base="ns1:…"`, внутри — только новое:

```xml
<xs:complexType name="Документ.X">
	<xs:complexContent>
		<xs:extension base="ns1:Документ.X">
			<xs:sequence>
				<xs:element name="МойРеквизит" type="xs:string" minOccurs="0"/>
				<xs:element name="МояТаблица" type="tns:Документ.X.МояТаблица" minOccurs="0"/>
			</xs:sequence>
		</xs:extension>
	</xs:complexContent>
</xs:complexType>
```

   Новая табличная часть — плюс два своих типа: контейнер и строка
   (см. пример «Лицензии» ниже). **Ничего из типового типа не копировать** —
   всё типовое наследуется само.
2. Переимпортировать пакет. Обязательно проверить, что у типа установлен
   «Базовый тип» (см. раздел «Диагностика»). Унаследованные свойства в списке
   свойств типа не видны — это норма.
3. Код: функция выгрузки пишется как для типового объекта (сценарий 1) —
   `СоздатьЗначениеXDTO("Документ.X")` вернёт расширенный тип, все типовые
   свойства заполняются как обычно. Новое — с защитой на случай неактивного
   расширения:

```bsl
	// новый простой реквизит
	Если ДанныеXDTO.Тип().Свойства.Получить("МойРеквизит") <> Неопределено Тогда
		УстановитьДанные(ДанныеXDTO, "МойРеквизит", ДокументОбъект.МойРеквизит);
	КонецЕсли;

	// новая табличная часть — строки только в СВОЙ контейнер, не в типовой!
	Если ДокументОбъект.МояТаблица.Количество() > 0 Тогда
		СтрокиXDTO = СоздатьЗначениеXDTO("Документ.X.МояТаблица");
		Для Каждого Строка Из ДокументОбъект.МояТаблица Цикл
			СтрокаXDTO = СоздатьЗначениеXDTO("Документ.X.МояТаблица.Строка");
			// ... заполнение полей строки ...
			СтрокиXDTO.Строка.Добавить(СтрокаXDTO);
		КонецЦикла;
		Если ДанныеXDTO.Тип().Свойства.Получить("МояТаблица") <> Неопределено Тогда
			УстановитьДанные(ДанныеXDTO, "МояТаблица", СтрокиXDTO);
		КонецЕсли;
	КонецЕсли;
```

4. Если нужно добавить колонки в **типовую** табличную часть: наследник строки
   `Документ.X.Товары.Строка` (base `ns1:Документ.X.Товары.Строка`) только с новыми
   полями. Код не меняется: `СоздатьЗначениеXDTO("Документ.X.Товары.Строка")` вернёт
   наследника, а наследника можно класть в типовой контейнер — платформа сама
   проставит `xsi:type` на строке.

### Две ошибки, по которым сразу видно нарушение сценария

- **«Тип свойства не соответствует типу значения»** — значение из одного пространства
  имён кладут в свойство, типизированное другим, и это не наследник. Правило:
  типовое — в типовое, новое — в своё, наследника — можно в базовое.
- **«Поле объекта не обнаружено (…)»** — у расширенного типа не установлен базовый
  тип (см. «Диагностика»), либо расширение неактивно и свойства действительно нет.

## Настройка пакета в расширении (конфигуратор)

Пакет расширения: `http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6/Extended`.

1. **Директива импорта.** В свойствах пакета расширения в поле «Директивы импорта»
   добавить `http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6`. После этого все типы
   типового пакета становятся доступны внутри расширенного: их можно указывать как
   типы свойств и как базовые типы.

2. **Доработка типового объекта** (например, `Документ.РеализацияТоваровУслуг`):
   - в расширенном пакете создать тип объекта **с тем же именем**;
   - в свойстве «Базовый тип» выбрать одноимённый тип из пространства имён `…/1.6`
     (он появится в списке выбора после п. 1);
   - в самом типе описать **только добавляемые** свойства (например, `Проект` с типом
     `КлючевыеСвойстваПроект` из расширенного пакета).

   Наследник получает все свойства базового типа, причём их типы остаются типами
   из 1.6 — поэтому типовые вложенные значения (`КлючевыеСвойства…`,
   `ОбщиеСвойстваДанныеВзаиморасчетов`, строки табличных частей и т.д.)
   присваиваются без ошибок.

3. **Ничего не копировать.** Полные копии типовых типов в расширенном пакете —
   источник конфликта, их нужно удалить. В расширенном пакете остаются только:
   - типы-наследники типовых (п. 2);
   - полностью новые типы (`Справочник.Проекты`, `КлючевыеСвойстваПроект` и т.п.).
     Если новый тип ссылается на типовую структуру (например, на
     `КлючевыеСвойстваОрганизация`), свойство типизируется импортированным типом
     из 1.6, а не копией.

4. **Доработка вложенных структур** (например, строки табличной части) делается так же:
   наследник `Документ.X.Товары.Строка` с базовым типом из 1.6. Значение-наследник
   допустимо присваивать свойству базового типа — платформа проставит `xsi:type`
   на вложенном элементе автоматически.

## Пример: новая табличная часть «Лицензии» у Документ.СчетПокупателю

Новая табличная часть — это связка из трёх типов в расширенной схеме:

1. Тип строки `Документ.СчетПокупателю.Лицензии.Строка` — самостоятельный тип; его поля
   могут ссылаться на типовые структуры через импорт (`ns1:КлючевыеСвойстваНоменклатура` и т.п.).
2. Тип-контейнер `Документ.СчетПокупателю.Лицензии` со свойством `Строка` этого типа.
3. **Наследник документа** `Документ.СчетПокупателю` (базовый тип — типовой из 1.6)
   с новым свойством `Лицензии` типа контейнера:

```xml
<xs:complexType name="Документ.СчетПокупателю">
	<xs:complexContent>
		<xs:extension base="ns1:Документ.СчетПокупателю">
			<xs:sequence>
				<xs:element name="Лицензии" type="tns:Документ.СчетПокупателю.Лицензии" minOccurs="0"/>
			</xs:sequence>
		</xs:extension>
	</xs:complexContent>
</xs:complexType>
```

В коде строки лицензий складываются в **свой** контейнер, и он присваивается свойству
`Лицензии`. Класть их в типовой контейнер «Услуги» нельзя: его список принимает только
`{…/1.6}Документ.СчетПокупателю.Услуги.Строка` и его наследников — иначе получите
«Тип свойства не соответствует типу значения».

После правки XSD пакет в расширении нужно переимпортировать: фабрика XDTO работает
с пакетом из конфигуратора, а не с файлом схемы в репозитории.

## Обязательность полей (minOccurs)

Элемент без `minOccurs="0"` обязателен. Если код заполняет свойство условно
(`Если ЗначениеЗаполнено(...) …`), у элемента в схеме должно стоять `minOccurs="0"`,
иначе запись объекта упадёт с ошибкой «значение свойства не установлено».
`nillable="true"` не заменяет `minOccurs="0"`: nillable разрешает пустое значение
установленного свойства, а не его отсутствие.

## Как это использует код модуля (EnterpriseData.bsl)

- `СоздатьЗначениеXDTO(Тип)` ищет тип сначала в расширенном пакете, затем в типовом.
  Если тип переопределён в расширении — объект автоматически создаётся расширенным,
  код заполнения при этом не меняется.
- `ЗаписатьОбъектXDTO(ФайлОбмена, ДанныеXDTO)` пишет объекты расширенных типов,
  унаследованных от типовых, элементом типового формата с атрибутом `xsi:type`;
  остальные объекты — как раньше.
- В `ВыгрузитьДанные` на элементе `Message` объявлен префикс `ext` для пространства
  имён расширения, чтобы `xsi:type="ext:…"` записывался компактно.

## Как выглядит результат

```xml
<Body xmlns="http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6">
	<!-- типовой объект — без изменений -->
	<Справочник.Валюты>…</Справочник.Валюты>

	<!-- доработанный объект: типовой элемент + xsi:type расширенного типа -->
	<Документ.РеализацияТоваровУслуг xsi:type="ext:Документ.РеализацияТоваровУслуг">
		<КлючевыеСвойства>…</КлючевыеСвойства> <!-- всё типовое как раньше -->
		…
		<Проект>…</Проект>                     <!-- реквизит из расширения -->
	</Документ.РеализацияТоваровУслуг>

	<!-- полностью новый объект живёт в пространстве имён расширения -->
	<ext:Справочник.Проекты>…</ext:Справочник.Проекты>
</Body>
```

## Чтение на стороне приёмника

- Если приёмник — 1С: в его конфигурации (или расширении) должны быть **оба** пакета
  с той же структурой. `ФабрикаXDTO.ПрочитатьXML(ЧтениеXML, ОжидаемыйТип)` с типовым
  ожидаемым типом по `xsi:type` вернёт объект расширенного типа со всеми
  добавленными свойствами.
- Если приёмник просто парсит XML — все поля доступны в одном элементе, изменение
  для него сводится к появлению новых вложенных элементов и атрибута `xsi:type`.

## Диагностика: «Поле объекта не обнаружено (КлючевыеСвойства)»

Ошибка означает, что документ создан по расширенному типу, но у типа в пакете
**не установлен базовый тип** — свойства типового документа не унаследовались.

Проверка в консоли кода:

```bsl
Тип = ФабрикаXDTO.Тип("http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6/Extended", "Документ.СчетПокупателю");
Сообщить(Тип.БазовыйТип); // должно вывести {…/1.6}Документ.СчетПокупателю
Сообщить(Тип.Свойства.Получить("КлючевыеСвойства") <> Неопределено); // Истина
```

Как починить в конфигураторе:

1. У пакета расширения в «Директивах импорта» должен быть URI
   `http://v8.1c.ru/edi/edi_stnd/EnterpriseData/1.6`.
2. У типа `Документ.СчетПокупателю` в палитре свойств в поле «Базовый тип»
   выбрать `Документ.СчетПокупателю` из пространства `…/1.6`.
3. Унаследованные свойства **не показываются** в списке свойств типа — это нормально.
   Копировать их в тип не нужно: в типе остаётся только своё (например, `Лицензии`).
4. Заодно проверить базовый тип у `Справочник.Проекты` / `Справочник.ПроектыГруппа` —
   `Object` из пакета `http://www.1c.ru/SSL/Exchange/Message`. Его потеря не роняет
   выгрузку (тип пустой), но лучше держать схему честной.

Почему так выходит при импорте XSD: `xs:import` без `schemaLocation` не даёт
конфигуратору найти типы 1.6 в момент импорта, и ссылка на базовый тип молча
теряется. В схему добавлен `schemaLocation="EnterpriseData_1_6_23.xsd"` — при
импорте держите оба файла в одном каталоге и после импорта проверяйте базовый тип.

## Ограничения

- Валидация файла по строгой типовой XSD 1.6 не пройдёт: наследование добавляет
  новые элементы, о которых типовая схема не знает. Приёмник должен использовать
  расширенную схему либо не валидировать строго.
- Типы-перечисления (`СтавкиНДС` и т.п.) наследованием не расширяются. Новое
  «перечисление» — это отдельный тип значения в расширенном пакете, используемый
  только в новых свойствах.
- Расширение должно быть установлено и активно в обеих базах, участвующих в обмене.
